<UserControl
    x:Class="Ink_Canvas_Better.Controls.ICB_SubPanel"
    x:Name="SubPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localControls="clr-namespace:Ink_Canvas_Better.Controls"
    xmlns:local="clr-namespace:Ink_Canvas_Better"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    mc:Ignorable="d" 
    d:DesignHeight="100"
    d:DesignWidth="200">

    <Grid
        VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition
                Name="GridRow0"
                Height="{Binding ElementName=TitleTextBlock, Path=Height}"/>
            <RowDefinition
                Name="GridRow1"/>
        </Grid.RowDefinitions>

        <Border
            SnapsToDevicePixels="True"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1,1,1,0"
            Background="{DynamicResource floatingBarBackground}"
            Grid.Row="0"
            CornerRadius="5,5,0,0">
            <Grid>
                <TextBlock
                    HorizontalAlignment="Left"
                    Name="TitleTextBlock"
                    Padding="5,2"
                    FontSize="16"
                    Text="{Binding Title, ElementName=SubPanel}">
                </TextBlock>

                <ikw:SimpleStackPanel
                    Margin="0,0,5,0"
                    HorizontalAlignment="Right"
                    Spacing="5"
                    Orientation="Horizontal">
                    <Button
                        Name="PinButton"
                        Click="PinButton_Click"
                        BorderThickness="0"
                        Background="Transparent">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True"/>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <ui:FontIcon
                            FontSize="16"
                            FontFamily="{DynamicResource FluentIconFontFamily}"
                            Glyph="&#xe840;"/>
                    </Button>
                
                    <Button
                        Name="CloseButton"
                        Click="CloseButton_Click"
                        BorderThickness="0"
                        Background="Transparent">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="IndianRed" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <ui:FontIcon
                            FontSize="16"
                            FontFamily="{DynamicResource FluentIconFontFamily}"
                            Glyph="&#xe8bb;"/>
                    </Button>
                </ikw:SimpleStackPanel>
            </Grid>
        </Border>

        <Border
            SnapsToDevicePixels="True"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1,0,1,1"
            Background="{DynamicResource floatingBarBackground}"
            Grid.Row="1"
            CornerRadius="0,0,5,5">
            <ItemsControl
                Margin="5"
                ItemsSource="{Binding Children, ElementName=SubPanel}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel
                            Name="contentPanel"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

        </Border>
        
    </Grid>
    
</UserControl>
